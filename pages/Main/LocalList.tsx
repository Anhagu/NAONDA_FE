import React from "react";
import styled from "styled-components";

const LocalList = () => {
  return(
    <LocalListContainer>
    </LocalListContainer>
  );
};

const LocalListContainer = styled.div`
  width: 30vw;
  height: 100%;

  position: absolute;
  top: 70;

  background-color: #fff;
`

export default LocalList;